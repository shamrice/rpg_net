#ifndef __COMMAND_TYPE__
#define __COMMAND_TYPE__

/*
 * Header file for command constants.
 * 
 */

enum CommandType {
    SHUTDOWN,
    ADD,
    GET,
    UPDATE,
    INFO,
    LIST,
    SET_ACTIVE,
    SET_INACTIVE
};

#endif